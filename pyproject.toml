[project]
name = "manifold-grad-proj"
version = "1.0.0"
license = { file = "LICENSE" }
dependencies = ["torch==2.12.0", "numpy", "matplotlib", "scipy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
